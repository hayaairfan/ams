@{
    ViewData["Title"] = "Dashboard";
}

<div class="container mt-4">
    <h2>@ViewBag.Message</h2>
    <p>You are logged in as: <strong>@User.Identity?.Name</strong></p>

    <div class="mt-3">
        <a href="/Students" class="btn btn-success">View Students Table</a>
        <a href="/Identity/Account/Manage" class="btn btn-primary">Change Password</a>
        <a href="/Identity/Account/Logout" class="btn btn-danger">Logout</a>
    </div>
</div>